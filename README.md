🚀 Руководство по запуску системы рекрутинга
Эта система состоит из двух Telegram-ботов (Candidate Bot, Recruiter Bot), базы данных PostgreSQL и Mock API для интеграции, запущенных в контейнерах.

📋 Требования
Чтобы запустить систему, на вашем компьютере должно быть установлено:

Docker и Docker Compose (или Docker Desktop).

Git (для клонирования проекта).

Python (для выполнения команд, не связанных с Docker).

🛠️ 1. Подготовка и настройка
Шаг 1: Скачивание проекта и настройка .env
Клонируйте репозиторий (если вы еще этого не сделали):

Bash

git clone <адрес_вашего_репозитория_на_github>
cd PythonProject
Создайте файл настроек: Убедитесь, что в корне проекта есть файл с названием .env (точка в начале обязательна!). Скопируйте туда все ваши переменные окружения, которые мы настроили:

Fragment kodu

# --- НАСТРОЙКИ POSTGRESQL (для Docker) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=recruitment_db
# ВАЖНО: DB_HOST и DB_PORT используются Python-кодом ВНУТРИ Docker
DB_HOST=db
DB_PORT=5432

# URL подключения для SQLAlchemy
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}

# --- ТОКЕНЫ И ЧАТЫ ---
CANDIDATE_BOT_TOKEN=8154293347:AAGaLwLADDcGPXMa-UL1HdU6NHI9igF27C8
RECRUITER_BOT_TOKEN=8249017712:AAEb6N6FbByoKy6TFQxViCL-YJ1CsSuQxXg
CHANNEL_USERNAME=@inno_recruiting
QC_CHAT_ID=-5023543787                 # ID вашего приватного QC-чата
ADMIN_IDS="[5779631738]"               # Ваш Telegram ID (для админ-команд)

# --- API МОК ---
RECRUITING_API_URL=http://mock_api:8001
Шаг 2: Полная очистка (Для гарантии)
Перед первым запуском или после сбоя всегда выполняйте полную очистку, чтобы удалить старые данные и кеш Docker:

Bash

# Останавливает, удаляет контейнеры, сети и тома с данными (-v)
docker-compose down -v --remove-orphans
🚀 2. Запуск системы
Система запускается в два этапа: сначала создание базы данных, затем запуск всех сервисов.

Шаг 3: Инициализация базы данных
Вам нужно создать таблицы в PostgreSQL, используя ваш исправленный код (init_db):

Bash

# Запуск контейнера Candidate Bot, который выполнит создание таблиц,
# а затем немедленно остановится.
docker-compose run --rm candidate_bot python -c "from core.db import init_db; import asyncio; asyncio.run(init_db())"
Шаг 4: Запуск всех сервисов
Запустите все три приложения (два бота и Mock API) и PostgreSQL в фоновом режиме:

Bash

# Флаг --build гарантирует, что все последние изменения кода будут включены
docker-compose up --build -d
Шаг 5: Проверка логов
Убедитесь, что все контейнеры работают и не упали сразу после старта:

Bash

docker-compose ps
# Должно показать 4 контейнера со статусом "Up"
Чтобы проверить, что боты готовы к работе, посмотрите их логи:

Bash

docker-compose logs candidate_bot recruiter_bot
# Должно быть видно: INFO:aiogram.dispatcher:Run polling for bot...
🧪 3. Сквозное тестирование
Ваша система готова к работе. Вы можете протестировать полный цикл!

Проверка Admin/Вакансий:

Отправьте /start своему Candidate Bot (токен CANDIDATE_BOT_TOKEN).

Отправьте /admin (если ваш TG ID есть в ADMIN_IDS).

Тест Quick Apply (FSM):

Начните отклик: /start → "📋 Вакансии" → "✈️ Откликнуться в Telegram".

Выберите вакансию (например, Python).

Пройдите FSM-цикл.

Проверка QC-чата и кнопок:

После завершения FSM проверьте ваш QC-чат (который слушает Recruiter Bot).

Вы должны увидеть структурированное уведомление с кнопками.

Нажмите "✅ Взять в работу". Сообщение должно обновиться, а Recruiter Bot должен обработать колбэк.